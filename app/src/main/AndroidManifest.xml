<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.windows.uscpool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HOME">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register" />
        <activity android:name=".functionselection" />
        <activity android:name=".driveriu" />
        <activity android:name=".driverregister" />
        <activity android:name=".driveriu2" />
        <activity android:name=".food" />
        <activity android:name=".opinion" />
        <activity android:name=".thanks" />
        <activity android:name=".passengerhope"></activity>
    </application>

</manifest>